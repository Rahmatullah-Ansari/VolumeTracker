<Application x:Class="BankingApp.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:BankingApp"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <FontFamily x:Key="MontserratRegular">pack://application:,,,/Montserrat/Montserrat-Regular.ttf#Montserrat</FontFamily>
        <FontFamily x:Key="MontserratSemibold">pack://application:,,,/Montserrat/Montserrat-SemiBold.ttf#Montserrat</FontFamily>
        <FontFamily x:Key="MontserratLight">pack://application:,,,/Montserrat/Montserrat-Light.ttf#Montserrat</FontFamily>
        <FontFamily x:Key="MontserratBold">pack://application:,,,/Montserrat/Montserrat-Bold.ttf#Montserrat</FontFamily>
        <FontFamily x:Key="MontserratMedium">pack://application:,,,/Montserrat/Montserrat-Medium.ttf#Montserrat</FontFamily>
        <Style TargetType="{x:Type TabItem}" x:Key="TabItemStyle">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TabItem}">
                        <Grid>
                            <Border Name="Border" BorderThickness="0" BorderBrush="Gainsboro" CornerRadius="5,5,0,0"
                            Margin="4">
                                <ContentPresenter x:Name="ContentPresenter" VerticalAlignment="Center" HorizontalAlignment="Stretch"
                                          ContentSource="Header" TextBlock.FontSize="18" />
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background"
                                        Value="#ffcccc" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="False">
                                <Setter Property="Foreground" Value="#003569" />
                                <Setter TargetName="Border" Property="Background"
                                        Value="white" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Padding" Value="0" />
        </Style>
    </Application.Resources>
</Application>
